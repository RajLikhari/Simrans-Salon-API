module.exports = (req,res) => {
    // id = req.query.id
    // result = db.findClientInDB(id)
    // result.then(function(result){
    //     res.send(result)
    // })
   
   


    // // id = req.query.id
    // // pass = req.query.pass
    // // if(id.localeCompare("raj") != 0 || pass.localeCompare("raj") != 0){
    // //     res.status(404).send("Forbidden")
    // //     console.log(id + " " + pass)
    // // } else {
    // //     res.render('delApp', {
    // //         firstName: "Raj",
    // //         lastName: "Likhari"
    // //     })
    // // }
}